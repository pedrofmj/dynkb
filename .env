# Server Configuration
# The port on which the server will listen for incoming requests
PORT=3000

# Database Configuration
# Type of database to use: "json" for file-based storage, "memory" for in-memory storage,
# or "mysql", "postgres" for external databases
DATABASE_TYPE=json
DATABASE_PATH=/c/development/egt/interviews_projects/dynkb/db/topics.js

# If using an external database (e.g., MySQL or PostgreSQL), specify the following:
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=your_user
DATABASE_PASSWORD=your_password
DATABASE_NAME=knowledge_base

# Logging Configuration
# Enable or disable logging (true or false)
ENABLE_LOGGING=true